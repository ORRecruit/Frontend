"use client";
import CustomLoader from "@/components/customLoader";
import React from "react";

const loading = () => {
  return (
    <div>
      <CustomLoader />
    </div>
  );
};

export default loading;
